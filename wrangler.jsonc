
{
  "name": "sound-recorder",
  "compatibility_date": "2026-01-08", // 不可为未来日期
  "main": "src/index.js",
  // 静态资产配置（核心：托管 public 中的 HTML 文件）
  "assets": {
    "directory": "./public", // 指向存放 HTML 的 public 目录
    "routes": [
      { "src": "/(.*)", "dest": "/$1" } // 匹配所有请求，转发到 public 中的文件
    ]
  },
// 【可选】是否开启开发模式（生产环境建议关闭）
  "dev": false,
  // 【可选】绑定的自定义域名（若有，可后续添加）
  "routes": []
}